/*
 * 冒泡排序算法实现
 * 文件名：lianxiti14.c
 * 功能：演示冒泡排序算法，包含详细注释
 * 算法原理：
 *   1. 重复遍历要排序的数列
 *   2. 每次遍历比较相邻的两个元素
 *   3. 如果顺序错误（前一个大于后一个）就交换它们
 *   4. 每次遍历后，最大的元素会"冒泡"到数列末尾
 *   5. 重复这个过程，直到整个数列有序
 */

#include <stdio.h>

/**
 * 冒泡排序函数
 * @param arr 待排序的数组
 * @param n 数组长度
 * 
 * 算法步骤详解：
 * 1. 外层循环控制排序轮数，共需要n-1轮
 * 2. 内层循环进行相邻元素比较和交换
 * 3. 每轮排序后，最大的元素会移动到数组末尾
 * 4. 优化：如果某一轮没有发生交换，说明数组已有序，可提前结束
 */
void bubbleSort(int arr[], int n) {
    int i, j, temp;
    int swapped; // 标记是否发生交换，用于优化
    
    // 外层循环：控制排序轮数，共需要n-1轮
    // 每轮排序会将当前未排序部分的最大值"冒泡"到末尾
    for (i = 0; i < n - 1; i++) {
        swapped = 0; // 每轮开始前重置交换标记
        
        // 内层循环：比较相邻元素并交换
        // 注意：每轮比较次数减少i次，因为最后i个元素已经有序
        for (j = 0; j < n - 1 - i; j++) {
            // 比较相邻的两个元素
            // 如果前一个元素大于后一个元素，说明顺序错误
            if (arr[j] > arr[j + 1]) {
                // 交换两个元素的位置
                temp = arr[j];      // 临时保存前一个元素
                arr[j] = arr[j + 1]; // 将后一个元素移到前一个位置
                arr[j + 1] = temp;   // 将临时保存的元素移到后一个位置
                
                swapped = 1; // 标记发生了交换
                
                // 调试信息：显示每次交换后的数组状态（可注释掉）
                // printf("交换 %d 和 %d: ", arr[j], arr[j+1]);
                // printArray(arr, n);
            }
        }
        
        // 优化：如果本轮没有发生任何交换，说明数组已经有序
        // 可以提前结束排序，减少不必要的比较
        if (swapped == 0) {
            // 调试信息：显示提前结束的轮数
            // printf("第 %d 轮后数组已有序，提前结束排序\n", i + 1);
            break;
        }
    }
}

/**
 * 打印数组函数
 * @param arr 要打印的数组
 * @param n 数组长度
 */
void printArray(int arr[], int n) {
    int i;
    printf("[");
    for (i = 0; i < n; i++) {
        printf("%d", arr[i]);
        if (i < n - 1) {
            printf(", ");
        }
    }
    printf("]\n");
}

/**
 * 主函数：演示冒泡排序的使用
 * 步骤：
 * 1. 定义测试数组
 * 2. 打印排序前的数组
 * 3. 调用冒泡排序函数
 * 4. 打印排序后的数组
 */
int main() {
    // 测试用例1：随机无序数组
    int arr1[] = {64, 34, 25, 12, 22, 11, 90};
    int n1 = sizeof(arr1) / sizeof(arr1[0]);
    
    printf("========== 冒泡排序算法演示 ==========\n\n");
    
    printf("测试用例1：随机无序数组\n");
    printf("排序前数组: ");
    printArray(arr1, n1);
    
    // 调用冒泡排序函数
    bubbleSort(arr1, n1);
    
    printf("排序后数组: ");
    printArray(arr1, n1);
    printf("\n");
    
    // 测试用例2：已排序数组（演示优化效果）
    int arr2[] = {1, 2, 3, 4, 5, 6, 7};
    int n2 = sizeof(arr2) / sizeof(arr2[0]);
    
    printf("测试用例2：已排序数组（演示优化）\n");
    printf("排序前数组: ");
    printArray(arr2, n2);
    
    bubbleSort(arr2, n2);
    
    printf("排序后数组: ");
    printArray(arr2, n2);
    printf("\n");
    
    // 测试用例3：逆序数组
    int arr3[] = {9, 8, 7, 6, 5, 4, 3, 2, 1};
    int n3 = sizeof(arr3) / sizeof(arr3[0]);
    
    printf("测试用例3：完全逆序数组\n");
    printf("排序前数组: ");
    printArray(arr3, n3);
    
    bubbleSort(arr3, n3);
    
    printf("排序后数组: ");
    printArray(arr3, n3);
    printf("\n");
    
    printf("========== 算法演示结束 ==========\n");
    printf("总结：\n");
    printf("1. 冒泡排序是稳定的排序算法（相等元素相对位置不变）\n");
    printf("2. 时间复杂度：最好情况O(n)，最坏情况O(n²)，平均情况O(n²)\n");
    printf("3. 空间复杂度：O(1)，是原地排序算法\n");
    printf("4. 优化后，对已排序数组只需一轮遍历即可结束\n");
    
    return 0;
}

/*
 * 算法复杂度分析：
 * 
 * 时间复杂度：
 * - 最好情况（数组已有序）：O(n)，只需一轮遍历
 * - 最坏情况（数组完全逆序）：O(n²)，需要n(n-1)/2次比较
 * - 平均情况：O(n²)
 * 
 * 空间复杂度：
 * - O(1)，只需要常数级别的额外空间（temp变量）
 * 
 * 稳定性：
 * - 稳定排序算法，相等元素不会交换位置
 * 
 * 适用场景：
 * - 小规模数据排序
 * - 教学演示排序算法原理
 * - 对稳定性有要求的简单排序任务
 */